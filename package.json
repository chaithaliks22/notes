{
  "name": "notenest-workspace",
  "version": "1.0.0",
  "description": "Full-Stack Notes Application (NoteNest) - Root Workspace",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node start_dev.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "seed": "node backend/seed.js",
    "test": "node backend/test_api.js",
    "build": "npm --prefix frontend run build",
    "build:all": "npm --prefix backend install --no-save && npm --prefix frontend install --no-save && npm --prefix frontend run build",
    "install:all": "npm --prefix backend install --no-save && npm --prefix frontend install --no-save"
  },
  "keywords": [
    "notes",
    "mern",
    "react",
    "express",
    "mongodb",
    "crud"
  ],
  "author": "",
  "license": "ISC"
}
